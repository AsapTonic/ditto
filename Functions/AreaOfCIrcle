
//create a circle object using the object literal syntax.

//circle.radius=2;

//circle object
const circle={
radius: 1,
// getter function that squares the radius of the circle whenever called
get area(){
return Math.PI * this.radius * this.radius;

}

};
console.log(circle.area);